1) remove this from D:\MyArduino\Projects\ESP32_Scale\PropaneScale\src\workflows\startup_tare_workflow.cpp.63:
Not-empty threshold: >= 35.50 lbs (tank tare + max propane + margin).
Stability tolerance: +/- 1.50 lbs once below not-empty threshold.
Timeout expiry with empty + stable readings auto-confirms taring workflow.

I WANT THIS:
Startup tare: waiting for empty scale...
Auto-detect is active.
Auto-detect timeout: 15 seconds
Send 'q' to skip startup tare.

COMPLETED

2) format & add runtime tare offset to output & remove "Propane Level Scale"
NB not sure all states & modes are being output. Check and add if necessary.

from this:

Propane Level Scale

Current Runtime Values
EEPROM ready: yes
Calibration factor: -10420.86
Known calibration weight: 26.00 lbs
Max propane weight: 16.00 lbs
Tank tare: 17.50 lbs
Calibration mode: 0 (NONE)
Calibration state: 0 (IDLE)
Level state: 0 (IDLE)
Tare state: 0 (IDLE)
Input mode: 0 (NONE)
Input state: 0 (IDLE)

to this:

Current Runtime Values
EEPROM ready: yes
Calibration factor: -10420.86
Known calibration weight: 26.00 lbs
Max propane weight: 16.00 lbs
Tank tare: 17.50 lbs
Runtime tare offset: 228748

Calibration mode: 0 (NONE)
Calibration state: 0 (IDLE)
Level state: 0 (IDLE)
Tare state: 0 (IDLE)
Input mode: 0 (NONE)
Input state: 0 (IDLE)

COMPLETED

3) all timeouts can be 15 seconds, so change settling to use CONFIRM_TIMEOUT_MS and remove CAL_SETTLE_DELAY_MS

LEAVE FOR NOW

4) verify InputContext inputCtx; is supposed to be in PropaneScale.ino

YES IT DOES. NEEDED by resetINputContext, WHICH MUST REMAIN IN PropaneScale.ino.

5) have all user input handled by 'handle*Input(char incoming) functions
to modify:
see note
Remove Serial Input Handling from `tickCalibration
calibration_workflow.cpp: handleCalibrationInput

currently existing:
input_known_weight.cpp: handleKnownWeightInput
input_propane_weight.cpp: handlePropaneWeightInput
input_tank_tare.cpp: handleTankTareInput
level_workflow.cpp: handleLevelReadInput

COMPLETED

6) Extract SKIP handling from `tickTare()` on PropaneScale.ino into a dedicated
input handler `handleStartupTareInput()` in the startup tare workflow module.

DONE

7) check if any scale.wait_ready_timeout usage is acceptable in web ble and/or wifi

NO. All have been removed.

8) AvgContext and ThresholdAvgContext are nearly identical.
Keep the minimumThreshold field present (unused) to avoid changing the struct layout and externs; leaving it zero-initialized is fine.
Update the declaration/definition where AvgContext avgCtx exists to ThresholdAvgContext avgCtx.
Verify all call sites and externs in workflows_contexts.h and scale_io.cpp so symbols match.
rename ThresholdAvgContext → AvgContext (single canonical type) for clearer intent.

DONE

9) get agent to propose but not implement:
a reentrant averaging logic level and calibration threshold averaging.
the level and workflow and 3 calibration workflows are never concurrent.
the averaging helper operates on a caller-supplied context so multiple independent samplers can share the same implementation without globals.
do not use a wrapper around non-blocking average units function.
analyze existing usage of non-blocking average units function to ensure proposed reentrant will not cause any regression bugs.

LEAVE FOR NOW

10)  Ask agent to:
a) Review level workflow pattern, calibration workflow pattern and startup tare pattern.
b) Review input known weight, max propane, and tank workflow patterns
c) review current runtime values, default eeprom, & help workflow patterns

What I want to know is where respective patterns are "out of sync"
and what ramifications are.

TODO

11) Startup tare timeout check: reading=0.00 lbs, baseline=0.00 lbs
I don't need to see this if reading & baseline are within established zero params.
I only need to see startup tare timeout check on negative weight or true positive weight

LEAVE

12) review all printing logic!

what costs more in ram/cycles:
this kind of print pattern:
Serial.print("Attempting STA connect to: ");
Serial.println(WIFI_SSID);

or this kind:
char buf[128];
unsigned long loadDetectSeconds = CONFIRM_TIMEOUT_MS / 1000UL;
snprintf(buf, sizeof(buf), "Waiting for weight placement on scale...\nSend 'q' to cancel.\nLoad placement timeout: %lu seconds.\n", loadDetectSeconds);
queueSerialOutput(buf);

Short answer: the snprintf path costs more — both RAM (stack) and CPU cycles — than the two-call Serial.print/println approach.

Why:

RAM: snprintf(buf,128,...) allocates the full buffer on the stack (here 128 bytes) plus snprintf's internal temporaries. Two Serial.print calls use essentially no extra stack (they read the strings from flash/data).
CPU: snprintf does parsing/formatting and integer -> ASCII conversion (divides/mods), which is relatively expensive. Two Serial.print calls do far less work (just stream the literal and the string/number to the driver).
Additional cost caveat: if queueSerialOutput() copies the string into a queue/heap, that adds heap/RAM; multiple small prints may avoid a large transient copy but do more driver calls.
Recommendation:

If you only concatenate a literal and a number, prefer multiple Serial.print calls (use F("...") for flash-stored literals) to minimize stack and cycles:
Serial.print(F("Waiting for weight placement on scale...\nSend 'q' to cancel.\nLoad placement timeout: "));
Serial.println(loadDetectSeconds);
Use snprintf only when you need complex formatting or a single contiguous string (or when you must queue a single buffer).

13) Replace manual JSON assembly with a lightweight JSON helper or library for robustness.
TO REVIEW
